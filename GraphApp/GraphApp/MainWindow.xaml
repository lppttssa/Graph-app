<Window x:Class="GraphApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="OpenFileDialogSample" Height="300" Width="1241.353">
    <Grid Background="#e2c9c3">
        <DockPanel Margin="10">
            <Button HorizontalAlignment="Left" VerticalAlignment="Top" Width="409" Height="30" FontFamily="Arial" 
                        FontSize="19" Foreground="#7e3d4a" BorderBrush="AliceBlue" Name="btnOpenFile"
                        Click="btnOpenFile_Click" Background="#bbcacc">
                    Open file
                </Button>
                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Width="400" Height="30" FontFamily="Arial" 
                    FontSize="19" Foreground="#7e3d4a" BorderBrush="AliceBlue" Name="btnShowStat"
                        Click="btnShowStat_Click" Background="#bbcacc">Show statistics</Button>

        </DockPanel>
        <lvc:CartesianChart Series="{Binding SeriesCollection}" LegendLocation="Right"  Margin="21,42,21,25" Loaded="CartesianChart_Loaded" >
            <lvc:CartesianChart.AxisY>
                <lvc:Axis Title="Sales" LabelFormatter="{Binding YFormatter}"></lvc:Axis>
            </lvc:CartesianChart.AxisY>
            <lvc:CartesianChart.AxisX>
                <lvc:Axis Title="Month" Labels="{Binding Labels}"></lvc:Axis>
            </lvc:CartesianChart.AxisX>
        </lvc:CartesianChart>
    </Grid>
</Window>
    
    
